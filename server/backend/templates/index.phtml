<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8"/>
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>PANDORA</title>
        <link href='//fonts.googleapis.com/css?family=Lato:300' rel='stylesheet' type='text/css'>
        <style>
            /* Body styling */
            body {
                margin: 0;
                padding: 0;
                display: flex;
                flex-direction: column;
                align-items: center;
                justify-content: center;
                height: 100vh;
                background: linear-gradient(135deg, #e8f5e9, #fff);
                font-family: "Helvetica Neue", Helvetica, Arial, sans-serif;
                color: #333;
                text-align: center;
                font-size: 18px;
                overflow: hidden;
            }
            
            /* Heading style */
            h1 {
                color: #4caf50;
                font-family: 'Lato', sans-serif;
                font-size: 90px;
                font-weight: 200;
                letter-spacing: -2px;
                margin: 0;
                transition: transform 0.3s ease-in-out;
            }

            /* Animation on hover */
            h1:hover {
                transform: scale(1.05);
                color: #388e3c;
            }

            /* Subtitle styling */
            .subtitle {
                color: #555;
                font-size: 20px;
                margin-top: 10px;
            }

            /* Link styling */
            a {
                display: inline-block;
                margin-top: 20px;
                padding: 10px 20px;
                background-color: #4caf50;
                color: white;
                font-size: 16px;
                text-decoration: none;
                border-radius: 5px;
                transition: background-color 0.3s ease;
            }

            /* Link hover effect */
            a:hover {
                background-color: #388e3c;
            }

            /* Subtle fade-in animation */
            @keyframes fadeIn {
                0% { opacity: 0; }
                100% { opacity: 1; }
            }

            h1, .subtitle, a {
                animation: fadeIn 1.5s ease-out;
            }
        </style>
    </head>
    <body>
        <h1>PANDORA</h1>
        <div class="subtitle">Automated Machine Learning Platform</div>
        <p><a href="https://atomic-lab.org">Explore aTomic Laboratory</a></p>
    </body>
</html>
